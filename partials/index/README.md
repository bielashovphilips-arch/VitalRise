# Index partials

Edit these files instead of editing the large generated `index.html` directly.

After changes, rebuild the page:

```powershell
powershell -ExecutionPolicy Bypass -File tools\build-index.ps1
```

Or with npm:

```powershell
npm run build:index
```
